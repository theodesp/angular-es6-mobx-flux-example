import template from './app.html';
import controller from './app.controller';
import './app.less';

const AppComponent = {
  template,
  controller,
  controllerAs: 'vm'
};

export default AppComponent;
